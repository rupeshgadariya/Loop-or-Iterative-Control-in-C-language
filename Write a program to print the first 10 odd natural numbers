// Write a program to print the first 10 odd natural numbers

#include<stdio.h>
#include<conio.h>

    void main()
{
        int x=1;
        while (x<10*2) 
  {  if (x%2!=0)
        printf("\n%lu",x);
        x++;
  }   
    getch();
}
